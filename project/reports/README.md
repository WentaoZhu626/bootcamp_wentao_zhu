# Reports

`final_report.md` is the stakeholder-ready written deliverable. Figures in `images/` and tables in
`../data/processed/` are generated by `notebooks/project_pipeline.ipynb` through reusable `src/` functions.
