# Model Artifact

`regime_model.joblib` contains the fitted Gaussian Hidden Markov Model, its training-only standardizer,
feature clipping bounds, state-level forward-return mapping, feature names, model version, and training date.
It is a research decision-support artifact, not an authorization to trade.
